#include "main.h"
/**
*main - the function excutabel
*Return: always 0 if os right
*/

int main(void)
{
_puts("\"At the end of the day, my goal was to be the best hacker\"\n");
_puts("\t- Kevin Mitnick");
return (0);
}
